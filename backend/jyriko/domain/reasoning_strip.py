"""Stub for removed reasoning_strip — v1 team system."""

from typing import Any


def build_reasoning_strip(
    pipeline: dict[str, Any],
    narrative: dict[str, Any],
    directives: dict[str, Any],
    scene: dict[str, Any],
    state: dict[str, Any],
) -> dict[str, Any]:
    """Build reasoning strip from pipeline components."""
    return {"steps": [], "summary": "Reasoning strip not yet implemented."}
