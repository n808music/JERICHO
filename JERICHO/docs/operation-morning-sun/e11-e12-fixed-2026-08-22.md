---
name: e11-e12-fixed-2026-08-22
description: "E11/E12 FIXED: Selector wiring + time-freezing. Full test suite running for baseline impact check."
metadata: 
  node_type: memory
  type: project
  modified: 2026-08-22T02:41:09.085Z
  originSessionId: f91674bd-7c3b-4909-8f30-810747e7e052
---

# E11/E12: Both Fixed ✅

## E11: Incomplete Block Not Removed (FIXED)

**Root cause:** `resolveBacklogBlocks` selector defined but never wired into `computeDerivedState` TICK_NOW handler.

**Fix implemented:**
1. Added import: `import { resolveBacklogBlocks } from '../core/engine/resolveBacklogBlocks.ts'`
2. Wired selector in TICK_NOW handler (after rollover completes):
   - Call `resolveBacklogBlocks(next)` to find blocks with MISSED events
   - Filter those blocks out of `next.today.blocks`
   - Store backlog block IDs in `next._backlogBlockIdsFromRollover` for later use
3. Updated `mergePriorTodayBlocks` to use cached backlog IDs when restoring "missing" blocks:
   - Prevents re-adding blocks that were intentionally filtered to backlog
   - Falls back to selector if cache not available

**Files modified:**
- `src/state/identityCompute.js`: Selector wiring in TICK_NOW (lines ~935-945), cache storage, mergePriorTodayBlocks update

**Test status:** ✅ PASSING (all 7 tests in midnightRollover.test.ts)

## E12: daysInBacklog Off-By-One (FIXED)

**Root cause:** Test fixture was missing `appTime.activeDayKey`, causing `resolveBacklogBlocks` to fall back to `new Date().toISOString()`, which was 2026-08-22 (system UTC date), not the intended 2026-08-21.

**Fix implemented:**
- Added `vi.useFakeTimers()` + `vi.setSystemTime(new Date('2026-08-21T12:00:00.000Z'))` to freeze test time
- Wrapped test in try/finally to ensure real timers restored
- Test is now decoupled from wall-clock date

**Files modified:**
- `src/core/engine/flowIncompleteToBacklog.test.ts`: Time-freezing wrapper around the failing test (lines ~397-420)

**Test status:** ✅ PASSING (10/10 active tests, 4 skipped)

## Verification: Independence Prediction Confirmed

Both fixes are independent:
- E11 wiring does NOT affect E12's test behavior (E12 constructs its own state, calls selector directly)
- Fixing E11 did NOT change E12's failure mode
- E12's fix (time-freezing) is purely test-side, doesn't affect production code

## Next Steps

1. **Full test suite running** — baseline impact check (comparing against frozen 36-failure baseline)
2. **Name-diff against baseline** — confirm no unexpected regressions
3. **Apply E9 fix** on clean baseline with E11/E12 cleared

## Related

[[e9-fix-BLOCKED-test-date-hardcoding]] — E9 fix ready to apply once baseline confirmed clean
